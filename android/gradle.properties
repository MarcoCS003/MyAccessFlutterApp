org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Flutter 3.44 activa R8/shrinkResources por defecto en release; R8 elimina
# los recursos de google-services (google_app_id, etc.) y Firebase no inicializa
# en el AAB de Play (app colgada en splash). Desactivado hasta estabilizar
# proguard-rules.pro.
shrink=false
